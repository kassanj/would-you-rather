Thank you for your purchase!

Hi <%= @user.first_name %>,

We’re excited to help you connect with our audience. Your payment has been processed successfully. A summary of your order is below. If you would like to make some changes, you may do so at: https://promotions.whereby.us/orders/<%= @order.id %>/edit.

Our team is now reviewing your promotion to ensure e-mail spam compliance. We'll reach out to you if we have any questions or concerns.

You can reach us by replying to this email if you have any questions!

Thanks,

<%= @order.set_site_name %>



Order Receipt for ID #<%= @order.id %>

<%= @order.created_at.strftime("%b %d, %Y") %>

Description

Package <%= @order.package.name %> Package
Amount: $<%= @order.final_amount ? @order.final_amount / 100 : 0 %>.00
Total: $<%= @order.final_amount ? @order.final_amount / 100 : 0 %>.00

Order Summary for ID #<%= @order.id %>

<% @promotions.each do |t| %>
<% if p.promo_type == 'standard' || p.promo_type == 'event' %>
	<%= t.id %> - <%= if t.date == '' ? 'Incomplete' : t.date %>
	<%= t.emoji %> <%= t.headline %>
	<%= t.text %>
	<%= t.link %><br>
<% else %>
	<%= t.id %> - <%= if t.date == '' ? 'Incomplete' : t.date %>
	<%= t.emoji %> <%= t.headline %>
	<%= t.text %>
	<%= t.link %>
	<%= t.logo %><br>
<% end %>


© 2017 WhereBy.Us. All rights reserved.

WhereBy.Us
1951 NW 7th Avenue
Suite 600
Miami, Florida 33136
